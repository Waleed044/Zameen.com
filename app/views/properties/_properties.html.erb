<%= search_form_for @q do |f|%>
  Search<%= f.search_field :owner_cont %>
  <%= f.submit %>
<% end %>

<table class="table table-striped table-bordered table-hover ">
  <thead class="thead-dark">
    <tr>
      <th scope="col">ID</th>
      <th scope="col">Owner</th>
      <th scope="col">Address</th>
      <th scope="col">Price</th>
      <th scope="col">Action</th>
    </tr>
  </thead>
  <tbody>
       <% @properties.each_with_index do | property, index | %>
      <tr>
        <td><%= index + 1 %></td>
        <td><%= property.owner %></td> 
        <td><%= property.address %></td>
        <td><%= property.price %></td>
        <td>    
  <div class="dropdown">
  <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
    Choose
  </button>  
  <ul class="dropdown-menu">
    <li><%= link_to 'Show', property_path(property),class: "btn btn-outline-secondary" %></li>
    <li><%= link_to 'Edit', edit_property_path(property),class:"btn btn-outline-secondary" %></li>
    <li><%= button_to 'Destroy', property_path(property),class: "btn btn-outline-secondary",
              method: :delete,
              data: { confirm: 'Are you sure?' }, remote: :true%>
    </li>
  </ul>
  </div></td>
    </tr>
    <% end %> 
  </tbody>
</table>
 
<style>
    .navbar{
    /* background-color:Lightgrey;   */
    position: fixed;
    top: 0;
    width: 100%;
    }
    table thead tr{
    background-color:Skyblue !important;
    color:white;
    }
    table {
            border-collapse: collapse;
            width: 100%;
          }
    th,td {
            text-align: left;
            padding: 8px;
          }
    tr:nth-child(even) {
            background-color: Lightgrey;
          }
    tr:nth-child(odd) {
            background-color: ;
    }
</style>
